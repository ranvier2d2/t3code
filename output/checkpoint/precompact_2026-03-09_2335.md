### Current Task State
Actively working on `feat/skills-without-session`: implementing session-independent skills loading via a lazy `JsonRpcProcessHandle`. Two commits landed (`dd7c7c4`, `c5f31fa`), PR #2 created on fork. Session ended mid-planning with three parallel exploration agents dispatched and a Plan agent invoked to design a combined implementation merging our approach with upstream `pingdotgg/t3code` PR #745.

### Critical Preservation Targets
- `apps/server/src/codexAppServerManager.ts`: new `JsonRpcProcessHandle` interface extracted from `CodexSessionContext`; `sendRequest`/`writeMessage`/`handleResponse` accept it; lazy `_skillsHandle` field; `ensureSkillsHandle()` method spawns/initializes a minimal codex app-server; `_codexBinaryPath`/`_codexHomePath` stored on first session start, reused in skills handle; `stopSkillsHandle()` called in `stopAll()`
- `apps/web/src/components/ChatView.tsx`: skill name label is `shrink-0` (no truncate), description has `min-w-0 truncate`
- `apps/web/src/lib/skillsReactQuery.ts`: unchanged — query calls `skills/list` RPC unchanged
- Upstream `pingdotgg/t3code` PR #745: compared; their approach uses a different architecture; plan was to combine the best of both

### Uncommitted Work State
Working tree is clean — only `output/` is untracked (build artifact directory, not source).

### Context Dependencies
- Upstream fork: `pingdotgg/t3code` PR #745 — skills loading reference implementation being studied for merge
- Our fork PR: `ranvier2d2/t3code` PR #2 — `feat/skills-without-session` (pushed, open)
- Plan file: `/Users/joaquinvenegasarevalo/.claude/plans/purring-swinging-rose.md` — combined implementation plan (in-progress, plan mode was entered)
- Codex App Server docs: `https://developers.openai.com/codex/sdk/#app-server`
- `skills/list` RPC only requires `initialize`+`initialized` handshake — no active session needed

### Error Recovery State
ERROR: `git add apps/server/src/codexAppServerManager.ts` from wrong cwd (`apps/web/`) → FIX: `cd /Users/joaquinvenegasarevalo/coding/T3CodeOG/t3code` first → STATUS: resolved
ERROR: Electron window never appeared in background task → FIX: user told to run `bun dev:desktop` in terminal directly → STATUS: workaround (GUI can't spawn from background tasks)

### Next Continuation Point
Read `/Users/joaquinvenegasarevalo/.claude/plans/purring-swinging-rose.md` to retrieve the combined implementation plan, then implement the merged design in `apps/server/src/codexAppServerManager.ts` incorporating whatever improvements from upstream PR #745 the plan specifies (three exploration agents had just finished; plan agent output was pending).
