### Current Task State
Feat `composer-skills-picker` is **complete and committed** (`4c238c7` on branch `feat/composer-skills-picker`). The `$` skill trigger was implemented end-to-end across contracts, server, and web. Working tree is clean.

### Critical Preservation Targets
- `packages/contracts/src/skill.ts` — NEW: `Skill`, `SkillsListInput`, `SkillsListResult` Effect schemas
- `apps/server/src/skillScanner.ts` — NEW: filesystem scanner that reads `SKILL.md` files from CWD subdirs
- `apps/web/src/lib/skillsReactQuery.ts` — NEW: TanStack Query `skillsQueryOptions(sessionId, cwd)`
- `packages/contracts/src/ws.ts` — added `skills.list` WS method + tagged request body entry
- `packages/contracts/src/ipc.ts` — added `skills` method to `NativeApi` interface
- `apps/server/src/codexAppServerManager.ts` — added `listSkills()` method + `skillInputs` in `sendTurn`; `no-map-spread` fix using `Object.assign` for params spread
- `apps/server/src/wsServer.ts` — wired `skills.list` route calling `skillScanner`
- `apps/web/src/wsNativeApi.ts` — added `skills` method calling WS `skills.list`
- `apps/web/src/components/ChatView.tsx` — `$` trigger type in `ComposerCommandItem`, skills query hook, skill selection handler, `EMPTY_SKILLS` constant, `Skill` import; `no-map-spread` fix
- `apps/web/src/composer-logic.ts` — added `$` as skill trigger (parallel to `@` files, `/` slash commands)
- `apps/web/src/composer-logic.test.ts` — 3 new tests for `$skill-trigger` detection (all 18 tests pass)

### Locked Decisions This Session
No formal locked-decisions table existed. Key architectural choices made:
- **Filesystem scanner over RPC**: `skillScanner.ts` reads `SKILL.md` directly rather than routing through codex app-server `skills/list` JSON-RPC — works without active sessions, simpler dependency graph.
- **`$` as skill trigger**: Mirrors `@` (file mentions) and `/` (slash commands) in composer; replaces `$query` with `$skill-name ` on selection.

### Context Dependencies
- Codex app-server skills docs: `https://developers.openai.com/codex/sdk/#skills` (skills are dirs with `SKILL.md`; `turn/start` accepts `{ type: "skill", name, path }` input items)
- turbo binary path: `node_modules/turbo/bin/turbo` (not in PATH by default in this env; oxlint at `node_modules/.bun/oxlint@1.51.0/node_modules/oxlint/bin/oxlint`)

### Error Recovery State
- ERROR: `exactOptionalPropertyTypes` on `skillPath: skill.path` (string|undefined) → FIX: conditional spread `...(skill.path ? { skillPath: skill.path } : {})` in `ChatView.tsx` → STATUS: resolved
- ERROR: `record.data` possibly undefined in `codexAppServerManager.ts` `listSkills` → FIX: added null-check guard → STATUS: resolved
- ERROR: `no-map-spread` lint on object spread inside `.map()` → FIX: `Object.assign({}, base, extra)` pattern → STATUS: resolved
- ERROR: pre-existing test failures (`composerDraftStore.test.ts`, `CheckpointReactor.test.ts`) → STATUS: pre-existing, unrelated to changes

### Next Continuation Point
Feature is done and committed. No immediate next step. If continuing, likely: push branch, open PR, or add UX polish (e.g., skill description in menu item, keyboard shortcut hint).
