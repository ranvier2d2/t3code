### Current Task State
Working on `fix/skills-reliability` branch applying 3 reliability fixes to `apps/server/src/codexAppServerManager.ts`. Tasks #1 (defer binary/home assignment), #2 (extract `cleanupSkillsHandle`), and #4 (route `fetchSkills` through cwd-matching session) were all set to `in_progress` and edits were actively being applied. Compaction fired during Fix #1 and Fix #2 edits — the file is modified but not committed.

### Critical Preservation Targets
- `apps/server/src/codexAppServerManager.ts`: partially-applied fixes for binary/home deferral (#1) and `cleanupSkillsHandle` extraction (#2); verify exact edit state before continuing
- `PLAN-skills-reliability.md`: committed in `d691fc1` — the authoritative spec for all 3 reliability fixes; read this to understand fix scope before editing
- PR #2 (squash commit `6c3f571`): merged `feat/skills-without-session` — all skills pipeline work is now on `main`; this branch (`fix/skills-reliability`) is purely for pre-existing reliability issues in `codexAppServerManager.ts`
- `packages/contracts/src/skill.ts`: `CodexSkillSelection` schema with `path` as optional — must stay optional to match `Skill.path`
- Dependency chain: Fix #3 (skillsHandle assignment before await) depends on Fix #2 being complete; Fix #5 (verify) depends on all others

### Uncommitted Work State
- `apps/server/src/codexAppServerManager.ts`: Partial application of Fix #1 (defer `this.codexBinaryPath`/`this.codexHome` assignment until after version validation) and Fix #2 (extract shared `cleanupSkillsHandle` method, rewrite all cleanup paths) — content is mid-edit, must read before further changes
- `output/`: Untracked background-agent output directory; safe to ignore

### Context Dependencies
- `PLAN-skills-reliability.md` (repo root, committed): Full spec for 3 fixes — read this first on resume
- Task tracker: Tasks #1, #2, #4 are `in_progress`; #3 blocked on #2; #5 blocked on all
- Pre-existing test failures (27): `storage.setItem is not a function` in zustand persist — confirmed pre-existing via `git stash` baseline, not caused by session changes
- CodeRabbit review feedback from PR #2 drove the reliability fixes; the pre-existing issues cited were: binary override timing, skills handle race, cross-cwd cache pollution, fetchSkills routing

### Error Recovery State
ERROR: `<tool_use_error>File has not been read yet` on ChatView.tsx edit → FIX: Read file first then re-applied edit → STATUS: resolved (changes committed in `d691fc1`'s predecessor)

### Next Continuation Point
Resume by reading the current state of `apps/server/src/codexAppServerManager.ts` to verify how much of Fix #1 and Fix #2 was applied before compaction. Then complete any remaining edits, apply Fix #4 (route `fetchSkills` through cwd-matching session rather than arbitrary first), mark #2 complete and unblock Fix #3 (assign `skillsHandle` before `await initialize`), then run `bun typecheck && bun lint` and commit all reliability fixes together.
